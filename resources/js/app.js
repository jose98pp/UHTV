/**
 * UHTV — JavaScript Entry Point (Vite)
 *
 * Configuración global de Axios para peticiones AJAX con CSRF de Laravel.
 * La lógica de UI (dark mode, menú móvil, BCB, clima) está en main.blade.php
 * para carga inmediata sin esperar al bundle.
 */
import './bootstrap.js';
